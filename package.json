{
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/liamross/psiagram",
  "homepage": "https://www.psiagram.org",
  "scripts": {
    "start": "parcel demo/index.html",
    "test": "jest --config ./scripts/test/jest.config.js",
    "build": "yarn clean-build && lerna run build",
    "bootstrap": "yarn build && lerna bootstrap",
    "clean-build": "node ./scripts/clean/builds.js",
    "clean-deep": "node ./scripts/clean/deep.js",
    "clean-all": "yarn clean-deep & yarn clean-build",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "@types/jest": "^23.0.0",
    "jest": "^23.1.0",
    "lerna": "^2.11.0",
    "parcel-bundler": "^1.8.1",
    "prettier": "^1.13.5",
    "rimraf": "^2.6.2",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.13.0",
    "typescript": "^2.9.1"
  }
}
